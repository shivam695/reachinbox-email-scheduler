"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createMetricsLatencyHandler = createMetricsLatencyHandler;
const errors_1 = require("../errors");
const METRICS = ['runtime', 'waittime'];
const DEFAULT_PERCENTILES = [50, 95, 99];
function createMetricsLatencyHandler(provider) {
    return async function metricsLatencyHandler(req) {
        var _a, _b, _c;
        const query = (_a = req === null || req === void 0 ? void 0 : req.query) !== null && _a !== void 0 ? _a : {};
        const metric = String((_b = query.metric) !== null && _b !== void 0 ? _b : '');
        if (!METRICS.includes(metric)) {
            return (0, errors_1.errorResponse)(400, 'ERRORS.INVALID_METRIC');
        }
        const granularity = query.granularity === 'hour' ? 'hour' : query.granularity === 'range' ? 'range' : 'day';
        const queue = typeof query.queue === 'string' && query.queue.length > 0 ? query.queue : undefined;
        const from = Number(query.from) || 0;
        const to = Number(query.to) || Date.now();
        const percentiles = String((_c = query.percentiles) !== null && _c !== void 0 ? _c : '')
            .split(',')
            .map((value) => value.trim())
            // Dropped before the coercion, because Number('') is 0 and 0 is now a legal percentile:
            // an absent `percentiles` would otherwise read as an explicit request for p0 rather
            // than falling back to the defaults.
            .filter((value) => value.length > 0)
            .map((value) => Number(value))
            // 0 is a real request, not a typo: it reads the floor of the distribution, the lower
            // edge of the first non-empty bucket. Excluding it silently swapped in the defaults.
            .filter((value) => Number.isFinite(value) && value >= 0 && value <= 100);
        const points = await provider.getLatency({
            queue,
            metric,
            from,
            to,
            granularity,
            percentiles: percentiles.length > 0 ? percentiles : DEFAULT_PERCENTILES,
        });
        return { status: 200, body: points };
    };
}
//# sourceMappingURL=metricsLatency.js.map