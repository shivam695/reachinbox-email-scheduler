"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createMetricsHistoryHandler = createMetricsHistoryHandler;
const errors_1 = require("../errors");
const GRANULARITIES = ['hour', 'day'];
const METRICS = ['completed', 'failed', 'queueage'];
/** Answered when no metric is named, which is what the board's throughput chart asks for. */
const DEFAULT_METRICS = ['completed', 'failed'];
function createMetricsHistoryHandler(provider) {
    return async function metricsHistoryHandler(req) {
        var _a, _b;
        const query = (_a = req === null || req === void 0 ? void 0 : req.query) !== null && _a !== void 0 ? _a : {};
        const granularity = (_b = query.granularity) !== null && _b !== void 0 ? _b : 'day';
        const queue = typeof query.queue === 'string' && query.queue.length > 0 ? query.queue : undefined;
        if (!GRANULARITIES.includes(granularity)) {
            return (0, errors_1.errorResponse)(400, {
                key: 'ERRORS.INVALID_GRANULARITY',
                options: { granularity: String(granularity) },
            });
        }
        const isPresent = (value) => (typeof value === 'string' && value.length > 0) || typeof value === 'number';
        if (!isPresent(query.from) || !isPresent(query.to)) {
            return (0, errors_1.errorResponse)(400, 'ERRORS.INVALID_DATE_RANGE');
        }
        const from = Number(query.from);
        const to = Number(query.to);
        if (!Number.isFinite(from) || !Number.isFinite(to) || to < from) {
            return (0, errors_1.errorResponse)(400, 'ERRORS.INVALID_DATE_RANGE');
        }
        let metrics = DEFAULT_METRICS;
        if (isPresent(query.metric)) {
            const requested = String(query.metric);
            if (!METRICS.includes(requested)) {
                return (0, errors_1.errorResponse)(400, 'ERRORS.INVALID_METRIC');
            }
            metrics = [requested];
        }
        const series = await Promise.all(metrics.map((metric) => provider.getHistory({ queue, metric, from, to, granularity })));
        const body = {};
        metrics.forEach((metric, i) => {
            body[metric] = series[i];
        });
        return { status: 200, body };
    };
}
//# sourceMappingURL=metricsHistory.js.map